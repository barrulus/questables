/**
 * Campaign Manager tests temporarily disabled.
 * TODO: Re-enable after migrating to fetch-free testing harness.
 */

describe.skip("CampaignManager", () => {
  it("is temporarily disabled pending fetch-free harness", () => {
    // Intentional skip placeholder.
  });
});
