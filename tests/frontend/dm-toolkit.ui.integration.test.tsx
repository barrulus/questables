/**
 * DM Toolkit UI integration tests temporarily disabled.
 * TODO: Re-enable once we have a deterministic harness that does not rely on fetch or external services.
 */

describe.skip("DM Toolkit UI integration", () => {
  it("is temporarily disabled pending fetch-free harness", () => {
    // Placeholder for intentional skip.
  });
});
