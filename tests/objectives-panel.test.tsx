/**
 * Objectives Panel tests temporarily disabled.
 * TODO: Re-enable when the panel can be exercised without fetch-based mocks.
 */

describe.skip("ObjectivesPanel", () => {
  it("is temporarily disabled pending fetch-free harness", () => {
    // Intentional skip placeholder.
  });
});
