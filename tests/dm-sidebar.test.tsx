/**
 * DM Sidebar tests temporarily disabled.
 * TODO: Re-enable once the sidebar can be exercised without fetch-based mocks.
 */

describe.skip("DMSidebar", () => {
  it("is temporarily disabled pending fetch-free harness", () => {
    // Placeholder so Jest reports skip intentionally.
  });
});
