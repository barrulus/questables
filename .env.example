# =============================================================================
# DATABASE CONFIGURATION
# Choose ONE of the following database options:
# =============================================================================

# Option 1: Supabase (Cloud PostgreSQL + Auth + Storage)
# VITE_SUPABASE_URL=https://your-project-id.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key-here
# VITE_SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Option 2: Local PostgreSQL (Self-hosted)
# If you set VITE_DATABASE_URL, it will override Supabase configuration
VITE_DATABASE_URL=postgresql://postgres:password@localhost:5432/dnd_app

# Alternative: Individual PostgreSQL connection parameters
# VITE_DATABASE_HOST=localhost
# VITE_DATABASE_PORT=5432
# VITE_DATABASE_NAME=dnd_app
# VITE_DATABASE_USER=postgres
# VITE_DATABASE_PASSWORD=password

# =============================================================================
# LOCAL DATABASE SERVER CONFIGURATION (for PostgreSQL option)
# =============================================================================

# Server configuration (for /server/ directory)
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=dnd_app
DATABASE_USER=postgres
DATABASE_PASSWORD=password
DATABASE_SSL=false

# Database server port (separate from PostgreSQL port)
DATABASE_SERVER_PORT=3001

# Frontend URL for CORS
FRONTEND_URL=http://localhost:3000